apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
helmCharts:
  - name: loki
    repo: oci://ghcr.io/grafana-community/helm-charts/loki
    version: 17.1.1
    releaseName: loki
    namespace: observability
    includeCRDs: true
    valuesFile: ./values.yaml
patchesStrategicMerge:
  - ./ignore-extraneous-diffs.yaml
