apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: grafana
  namespace: observability
spec:
  endpoints:
    - dnsName: grafana.${SECRET_DOMAIN}
      recordType: CNAME
      targets:
        - oauth.${SECRET_DOMAIN}
